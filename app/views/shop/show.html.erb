<div class="header text-center text-warning">
  <h1>Welcome to the Royale Shop</h1>
</div>

<div class="products">
  <% @products.each do |product| %>
  <div id="product-<%= product.id %>">
    <%= "#{product.title} - #{product.description} - #{product.price}" %>
  </div>
  <% end %>
</div>
